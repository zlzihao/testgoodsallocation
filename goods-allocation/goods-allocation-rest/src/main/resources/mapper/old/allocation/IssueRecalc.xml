<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.nome.saas.allocation.repository.old.allocation.dao.IssueRecalcMapper">

    <delete id="del_issue_category_data">
        DELETE FROM issue_category_data WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_detail">
        DELETE FROM issue_detail WHERE InShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_goods_data">
        DELETE FROM issue_goods_data WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_in_stock">
        DELETE FROM issue_in_stock WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_mid_category_qty">
        DELETE FROM issue_mid_category_qty WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_midcategory_data">
        DELETE FROM issue_midcategory_data WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_need_stock">
        DELETE FROM issue_need_stock WHERE ShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_out_stock_remain">
        DELETE FROM issue_out_stock_remain WHERE TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_undo">
        DELETE FROM issue_undo WHERE InShopID = #{shopId} AND TaskId = #{taskId}  AND Status = 2
    </delete>

    <delete id="del_issue_out_stock_remain_by_taskId">
        DELETE FROM issue_out_stock_remain WHERE TaskId = #{taskId}
    </delete>

</mapper>